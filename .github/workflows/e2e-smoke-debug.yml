name: E2E Smoke Tests Debug

on:
  push:
    branches: [ feature/repo-cleanup ]
  workflow_dispatch:
    inputs:
      runner:
        description: 'Runner to use (self-hosted or ubuntu-latest)'
        required: false
        default: 'self-hosted'
        type: choice
        options:
          - self-hosted
          - ubuntu-latest

jobs:
  debug:
    runs-on: ${{ github.event.inputs.runner || 'self-hosted' }}
    
    # Docker services chỉ work với GitHub-hosted runners
    # Self-hosted runners cần MySQL được setup locally hoặc via steps
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: zenamanage_e2e
          MYSQL_USER: e2e_user
          MYSQL_PASSWORD: e2e_password
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
        # Only use service for GitHub-hosted runners
        if: github.event.inputs.runner == 'ubuntu-latest' || (github.event.inputs.runner == '' && github.runner.os == 'Linux')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Install Playwright browsers
      run: npx playwright install --with-deps
      
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, dom, fileinfo, mysql, sqlite3, pdo_sqlite, bcmath, soap, intl, readline, libxml, zip, curl, gd, iconv, imagick, redis, memcached, pcov
        coverage: none
        
    - name: Install Composer dependencies
      run: composer install --no-progress --prefer-dist --optimize-autoloader
      
    - name: Check for .env.example file
      id: check-env
      run: |
        if [ ! -f .env.example ]; then
          echo "⚠️  WARNING: .env.example not found! Creating default .env..."
          cat > .env << 'EOF'
        APP_NAME=ZenaManage
        APP_ENV=local
        APP_KEY=
        APP_DEBUG=true
        APP_URL=http://localhost
        
        LOG_CHANNEL=stack
        LOG_DEPRECATIONS_CHANNEL=null
        LOG_LEVEL=debug
        
        DB_CONNECTION=mysql
        DB_HOST=127.0.0.1
        DB_PORT=3306
        DB_DATABASE=zenamanage_e2e
        DB_USERNAME=e2e_user
        DB_PASSWORD=e2e_password
        
        CACHE_DRIVER=array
        SESSION_DRIVER=array
        QUEUE_CONNECTION=sync
        EOF
          echo "env_created=true" >> $GITHUB_OUTPUT
        else
          cp .env.example .env
          echo "env_created=false" >> $GITHUB_OUTPUT
        fi
        
    - name: Generate application key
      run: php artisan key:generate
      
    - name: Configure database for CI
      run: |
        # Check if SQLite was configured in previous step (self-hosted without MySQL)
        if grep -q "DB_CONNECTION=sqlite" .env; then
          echo "✅ Already configured for SQLite (self-hosted)"
        else
          echo "DB_CONNECTION=mysql" >> .env
          echo "DB_HOST=127.0.0.1" >> .env
          echo "DB_PORT=3306" >> .env
          echo "DB_DATABASE=zenamanage_e2e" >> .env
          echo "DB_USERNAME=e2e_user" >> .env
          echo "DB_PASSWORD=e2e_password" >> .env
          echo "CACHE_DRIVER=array" >> .env
          echo "SESSION_DRIVER=array" >> .env
          echo "QUEUE_CONNECTION=sync" >> .env
        fi
        
    - name: Clear Laravel config cache
      run: |
        php artisan config:clear || true
        php artisan cache:clear || true
        
    - name: Verify database configuration
      run: |
        echo "📊 Verifying database configuration..."
        php artisan tinker --execute="
          echo 'Default DB: ' . config('database.default') . PHP_EOL;
          echo 'DB Connection: ' . config('database.connections.mysql.driver') . PHP_EOL;
          echo 'DB Host: ' . config('database.connections.mysql.host') . PHP_EOL;
          echo 'DB Database: ' . config('database.connections.mysql.database') . PHP_EOL;
        " || {
          echo "⚠️  Tinker failed, checking .env file directly:"
          cat .env | grep "^DB_"
        }
        
    - name: Setup MySQL for self-hosted runner
      if: github.event.inputs.runner == 'self-hosted' || (github.event.inputs.runner == '' && github.runner.os != 'Linux')
      run: |
        echo "🔍 Detecting MySQL setup for self-hosted runner..."
        
        # Check if MySQL is already running
        if mysql -h 127.0.0.1 -u root -e "SELECT 1" 2>/dev/null; then
          echo "✅ MySQL is already running locally"
          # Create database and user if needed
          mysql -h 127.0.0.1 -u root << 'SQL'
            CREATE DATABASE IF NOT EXISTS zenamanage_e2e;
            CREATE USER IF NOT EXISTS 'e2e_user'@'localhost' IDENTIFIED BY 'e2e_password';
            GRANT ALL PRIVILEGES ON zenamanage_e2e.* TO 'e2e_user'@'localhost';
            FLUSH PRIVILEGES;
          SQL
          echo "✅ Database and user created/verified"
        elif command -v brew &> /dev/null && brew services list | grep -q mysql; then
          echo "📦 Starting MySQL via Homebrew..."
          brew services start mysql || true
          sleep 5
          mysql -h 127.0.0.1 -u root << 'SQL'
            CREATE DATABASE IF NOT EXISTS zenamanage_e2e;
            CREATE USER IF NOT EXISTS 'e2e_user'@'localhost' IDENTIFIED BY 'e2e_password';
            GRANT ALL PRIVILEGES ON zenamanage_e2e.* TO 'e2e_user'@'localhost';
            FLUSH PRIVILEGES;
          SQL
          echo "✅ MySQL started and configured"
        else
          echo "⚠️  MySQL not found. Using SQLite for self-hosted runner..."
          echo "DB_CONNECTION=sqlite" >> .env
          echo "DB_DATABASE=$(pwd)/database/database.sqlite" >> .env
          touch database/database.sqlite
          echo "✅ Configured to use SQLite"
        fi
        
    - name: Wait for MySQL to be ready
      if: github.event.inputs.runner == 'ubuntu-latest' || (github.event.inputs.runner == '' && github.runner.os == 'Linux')
      run: |
        echo "Waiting for MySQL service to be ready..."
        for i in {1..30}; do
          if mysql -h 127.0.0.1 -u e2e_user -pe2e_password -e "SELECT 1" 2>/dev/null; then
            echo "✅ MySQL is ready!"
            break
          fi
          echo "⏳ Waiting for MySQL... (attempt $i/30)"
          sleep 2
        done
        
    - name: Wait for MySQL (self-hosted)
      if: github.event.inputs.runner == 'self-hosted' || (github.event.inputs.runner == '' && github.runner.os != 'Linux')
      run: |
        echo "Waiting for MySQL to be ready (self-hosted)..."
        for i in {1..30}; do
          if mysql -h 127.0.0.1 -u e2e_user -pe2e_password -e "SELECT 1" 2>/dev/null || mysql -h 127.0.0.1 -u root -e "SELECT 1" 2>/dev/null; then
            echo "✅ MySQL is ready!"
            break
          fi
          echo "⏳ Waiting for MySQL... (attempt $i/30)"
          sleep 2
        done
        
    - name: Test MySQL connection
      run: |
        echo "Testing MySQL connection..."
        mysql -h 127.0.0.1 -u e2e_user -pe2e_password -e "SELECT 1 as test;" || {
          echo "❌ MySQL connection failed!"
          exit 1
        }
        echo "✅ MySQL connection successful!"
        
    - name: Create database
      run: |
        # Ensure we're using MySQL
        php artisan config:clear
        php artisan cache:clear
        
        # Verify DB connection one more time before migration
        php artisan tinker --execute="
          \$driver = config('database.default');
          echo 'Using database driver: ' . \$driver . PHP_EOL;
          if (\$driver !== 'mysql') {
            echo 'ERROR: Expected mysql but got: ' . \$driver . PHP_EOL;
            exit(1);
          }
          echo '✅ Database driver verified as MySQL' . PHP_EOL;
        "
        
        # Run migrations
        php artisan migrate:fresh --force || {
          echo "❌ Migration failed! Checking database configuration..."
          php artisan tinker --execute="
            echo 'DB Default: ' . config('database.default') . PHP_EOL;
            echo 'MySQL Driver: ' . config('database.connections.mysql.driver') . PHP_EOL;
            echo 'MySQL Host: ' . config('database.connections.mysql.host') . PHP_EOL;
            echo 'MySQL Database: ' . config('database.connections.mysql.database') . PHP_EOL;
          "
          echo "📄 Current .env DB settings:"
          grep "^DB_" .env || echo "No DB_* settings found in .env"
          exit 1
        }
        
    - name: Seed database
      run: php artisan db:seed --class=E2EDatabaseSeeder --force
      
    - name: Verify database setup
      run: |
        echo "Database connection test:"
        php artisan tinker --execute="echo 'DB Connection: ' . config('database.default'); echo 'DB Host: ' . config('database.connections.mysql.host'); echo 'DB Database: ' . config('database.connections.mysql.database');"
        echo "User count:"
        php artisan tinker --execute="echo 'Users: ' . App\Models\User::count();"
        
    - name: Test environment variables
      run: |
        if [ -z "${{ secrets.SMOKE_ADMIN_EMAIL }}" ]; then
          echo "⚠️  WARNING: SMOKE_ADMIN_EMAIL secret is not set!"
          echo "   → Using default: admin@zena.local"
          echo "env_email=admin@zena.local" >> $GITHUB_ENV
        else
          echo "✅ SMOKE_ADMIN_EMAIL is set"
          echo "env_email=${{ secrets.SMOKE_ADMIN_EMAIL }}" >> $GITHUB_ENV
        fi
        
        if [ -z "${{ secrets.SMOKE_ADMIN_PASSWORD }}" ]; then
          echo "⚠️  WARNING: SMOKE_ADMIN_PASSWORD secret is not set!"
          echo "   → Using default: password"
          echo "env_password=password" >> $GITHUB_ENV
        else
          echo "✅ SMOKE_ADMIN_PASSWORD is set"
          echo "env_password=${{ secrets.SMOKE_ADMIN_PASSWORD }}" >> $GITHUB_ENV
        fi
        
    - name: Verify Laravel server can respond
      run: |
        echo "🔍 Testing if Laravel server responds..."
        # Start server in background for quick check
        php artisan serve --host=127.0.0.1 --port=8000 > /tmp/laravel-server.log 2>&1 &
        SERVER_PID=$!
        echo "Server PID: $SERVER_PID" >> $GITHUB_ENV
        
        # Wait for server to be ready (max 30 seconds)
        echo "⏳ Waiting for server to start..."
        for i in {1..30}; do
          if curl -f http://127.0.0.1:8000/health 2>/dev/null || curl -f http://127.0.0.1:8000 2>/dev/null; then
            echo "✅ Laravel server is responding!"
            break
          fi
          if [ $i -eq 30 ]; then
            echo "❌ Laravel server failed to start after 30 seconds!"
            echo "Server logs:"
            tail -20 /tmp/laravel-server.log || true
            kill $SERVER_PID 2>/dev/null || true
            exit 1
          fi
          sleep 1
        done
        
        # Verify login page specifically
        echo "🔍 Verifying login page is accessible..."
        LOGIN_RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/login)
        if [ "$LOGIN_RESPONSE" = "200" ]; then
          echo "✅ Login page returns 200 OK"
          
          # Check if page contains email input
          LOGIN_CONTENT=$(curl -s http://127.0.0.1:8000/login)
          if echo "$LOGIN_CONTENT" | grep -q 'id="email"\|name="email"\|#email'; then
            echo "✅ Login page contains email input field"
          else
            echo "⚠️  WARNING: Login page might not have email input"
            echo "Page snippet:"
            echo "$LOGIN_CONTENT" | head -50
          fi
        else
          echo "⚠️  WARNING: Login page returned status: $LOGIN_RESPONSE"
        fi
        
        # Don't kill server - let Playwright manage it or reuse it
        
    - name: Run smoke tests
      run: npm run test:e2e:smoke
      env:
        CI: true
        SMOKE_ADMIN_EMAIL: ${{ secrets.SMOKE_ADMIN_EMAIL != '' && secrets.SMOKE_ADMIN_EMAIL || 'admin@zena.local' }}
        SMOKE_ADMIN_PASSWORD: ${{ secrets.SMOKE_ADMIN_PASSWORD != '' && secrets.SMOKE_ADMIN_PASSWORD || 'password' }}
      continue-on-error: false
      
    - name: Upload test artifacts
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: e2e-test-results
        path: |
          test-results/**/*.png
          test-results/**/*.html
          test-results/**/*.json
          test-results/**/*.xml
        retention-days: 7
        
    - name: Upload Playwright report
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 7
