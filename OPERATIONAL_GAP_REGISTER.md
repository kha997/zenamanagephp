# Operational Gap Register

*Bắt buộc bởi `PROJECT_CONSTITUTION.md` mục 4 (Operational Gap Detection). Đây là nơi hợp nhất mọi lỗ thủng vận hành đã phát hiện — không tự động sửa, chỉ ghi nhận, chấm điểm, và xếp thứ tự theo mục 5 (Priority Rule).*

*Tạo lần đầu: 2026-07-23, hợp nhất từ 14 file trong `docs/audits/*` (2026-03-19 → 2026-07-09) và `docs/roadmap/backlog.yaml`. Đã spot-check lại tình trạng hiện tại (route/file/controller) cho các mục có khả năng đã fix, thời điểm 2026-07-23 — repo HEAD nhánh `main`.*

## Cách đọc bảng này

- **Priority tier** = số theo mục 5 hiến pháp (1 = quan trọng nhất). Bảng được nhóm theo tier.
- **Status**:
  - `OPEN (verified)` — đã kiểm tra lại code hôm nay, xác nhận vẫn tồn tại.
  - `RESOLVED (verified)` — đã kiểm tra lại, xác nhận đã fix từ khi audit gốc viết; giữ trong register làm lịch sử, không cần hành động.
  - `UNVERIFIED` — chưa re-check trong lần hợp nhất này; coi như `ASSUMPTION: có thể vẫn còn`, cần verify trước khi tin hoặc hành động theo mục 3 hiến pháp.
  - `BLOCKED (external)` — không thể đóng chỉ bằng code, cần xác nhận từ người ngoài repo (vd Slack admin).
  - `PARTIALLY RESOLVED (verified)` — gap cha chứa các phát hiện con đã xác minh riêng biệt, có trạng thái khác nhau: ít nhất một phần đã đóng, ít nhất một phần chưa đóng hoặc đang chờ xác nhận tái hiện. Các dòng con mới là nơi có thẩm quyền để hành động, không phải dòng cha.
  - `REOPENED FOR REPRODUCTION` — đã tìm thấy một nơi khả nghi hoặc một ứng viên đang hoạt động thật, nhưng CHƯA thực hiện bước tái hiện lỗi. Trạng thái này KHÔNG có nghĩa là đã xác nhận có lỗi thật, và KHÔNG cấp phép triển khai sửa — cần một quyết định Gate 1 riêng cho bước tái hiện trước khi coi là gap thật.
  - `CLOSED — NOT REPRODUCED (verified)` — suspected/candidate defect was investigated on a defined baseline, could not be demonstrated, and no remediation implementation is claimed.
  - `CLOSED (verified)` — parent/cluster terminal state where all children are terminal and no actionable child remains.
- **Mọi dòng đều có nguồn trích dẫn file:line** theo mục 8 hiến pháp — không có claim không bằng chứng.

---

## Tier 1 — Tính toàn vẹn dữ liệu & Tenant Isolation

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-001 | Cross-tenant IDOR trong Web `TaskController`/`DocumentController` (đợt migrate G7) | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:33`; xác minh Wave 1: `app/Http/Controllers/Web/TaskController.php:145,168`, `DocumentController.php:64,140,181` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá 2026-07-09)`.* `show/edit`/`show/approvals/create` từng thiếu tenant scope, ghi nhận "đã tenant-scope toàn bộ". **Xác minh Wave 1 (2026-08-06):** đã đọc trực tiếp mã nguồn hiện tại — tenant-scope áp dụng đầy đủ trên mọi route thật đang hoạt động; 2 method debug không tenant-scope nhưng không được route tới (mã còn sót lại, không dùng tới). |
| GAP-002 | Route gốc `/projects`, `/tasks`, `/documents` là closure JSON song song với `/app/*` chính danh — `POST /tasks` trả `{"message":"Task created"}` **mà không lưu DB thật** | **RESOLVED (verified)** | `routes/web.php:484,510,519,529,545,549,553` | Đây là bẫy toàn vẹn dữ liệu nghiêm trọng: người dùng/gọi API tưởng đã tạo task nhưng không có gì được ghi. Không phải chỉ là "dead code" — nó **trả response thành công giả**. Đề xuất: xoá các route/closure này hoặc redirect về `/app/*` thật, có test chặn hồi quy. Đã sửa 2026-07-23: `POST /tasks` nay delegate sang `Web\TaskController::store` (giống `/app/tasks`), có `rbac:task.create`. Xem `tests/Feature/Legacy/LegacyTaskCreationPersistsTest.php`. Lưu ý: `POST /projects`, `PUT/DELETE /projects/{project}`, `GET /projects/{project}` root-level vẫn còn trùng lặp kiến trúc (không fake, nhưng vẫn nên dọn sau) — chưa nằm trong phạm vi sửa lần này. |
| GAP-003 | Provenance bảng `zena_submittals` vs `submittals` chưa rõ ràng (alias runtime trỏ khác migration tạo) | **RESOLVED (verified)** | `docs/audits/2026-03-19-system-review-roadmap-repair.md:151`; xác minh Wave 1: `database/migrations/2025_09_16_083456_deprecate_zena_design_construction_tables.php:75-86`, `database/migrations/2026_07_07_000001_create_submittals_table.php` | *Trạng thái trước Wave 1: `UNVERIFIED`.* Submittals gate phê duyệt vật tư/procurement — dữ liệu sai bảng ảnh hưởng cost-reconciliation (tier 5) lẫn toàn vẹn dữ liệu. **Xác minh Wave 1 (2026-08-06):** `zena_submittals` đã được khai báo deprecated rõ ràng trong migration; bảng chính thức `submittals` và model `Submittal` duy nhất; không còn tham chiếu sống nào tới bảng cũ. |

## Tier 2 — Bảo mật, phân quyền, auditability

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-004 | Viewer role có thể tạo task qua `POST /app/tasks` do thiếu `rbac:task.create` | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:38-41`; xác minh Wave 1: `routes/web.php:396`, `tests/Feature/RBACRolesPermissionsTest.php` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá 2026-07-09)`.* Đã vá 5 route + có regression test theo audit gốc; PR#220 (22-23/07) tiếp tục hardening RBAC. **Xác minh Wave 1 (2026-08-06):** middleware `rbac:task.create` có mặt trên route thật, có test hồi quy đang chạy. |
| GAP-005 | SSRF qua webhook URL (cloud metadata / mạng nội bộ, kể cả DNS rebinding) | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:23`; xác minh Wave 1: `app/Http/Controllers/Web/WebhookPageController.php:38-42`, `app/Jobs/DeliverWebhook.php:97-114` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá)`.* Vá 2 lớp (create-time + DNS-resolve-time). **Xác minh Wave 1 (2026-08-06):** hai lớp chặn độc lập được xác nhận trong mã hiện tại — chặn lúc tạo webhook, và chặn lại lần nữa ngay lúc gửi thật (chống trường hợp địa chỉ đổi hướng sau khi qua lớp chặn đầu). |
| GAP-006 | Race condition tạo trùng daily log → lỗi 500 (TOCTOU) | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:24`; xác minh Wave 1: `database/migrations/2026_07_08_100000_create_site_diaries_table.php:41`, `app/Http/Controllers/Api/SiteDiaryController.php:198-207` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá)`.* Vá bằng unique constraint DB + catch QueryException 23000 → 422. **Xác minh Wave 1 (2026-08-06):** ràng buộc unique và cách bắt lỗi đều xác nhận có mặt trong mã hiện tại. |
| GAP-007 | Webhook retry đếm trùng lần thất bại (4 lần thay vì đúng số cấu hình) | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:25`; xác minh Wave 1: `app/Jobs/DeliverWebhook.php:73-75,85-87` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá)`.* Vá bằng guard `attempts < tries`. **Xác minh Wave 1 (2026-08-06):** điều kiện chặn đúng cả hai đường thử lại trong mã hiện tại. |
| GAP-008 | LIKE-filter injection trong tìm kiếm activity feed (`%`/`_` chưa escape) | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:26`; xác minh Wave 1: `app/Http/Controllers/Web/ActivityFeedPageController.php:26-27` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá)`.* **Xác minh Wave 1 (2026-08-06):** ký tự đặc biệt được thoát trước khi đưa vào câu truy vấn trong mã hiện tại. |
| GAP-009 | Tạo API token không có rate limit | **RESOLVED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:27`; xác minh Wave 1: `routes/web.php:952` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá)`.* Vá bằng `throttle:6,1`. **Xác minh Wave 1 (2026-08-06):** giới hạn tần suất có mặt trên route thật trong mã hiện tại. |
| GAP-010 | Cụm lỗi nhỏ: CSV formula injection, lộ secret qua flash message, OOM khi export, lệch timezone Gantt | **CLOSED (verified)** | `docs/audits/2026-07-09-acceptance-review.md:28` | *Trạng thái trước Wave 1: `UNVERIFIED (audit nói đã vá toàn bộ)`.* **Xác minh Wave 1 (2026-08-06):** xác minh riêng từng phần cho kết quả KHÁC NHAU — không thể coi cả cụm là một trạng thái duy nhất. Xem 3 dòng con GAP-010a/GAP-010b/GAP-010c ngay bên dưới — các dòng con là nơi có thẩm quyền để hành động. Tất cả dòng con đã terminal: GAP-010a RESOLVED, GAP-010b RESOLVED, GAP-010c CLOSED — NOT REPRODUCED. |
| GAP-010a | (Tách từ GAP-010) Đường xuất báo cáo chính thức (đường người dùng thật sự dùng): CSV formula injection + lộ secret qua flash message | **RESOLVED (verified 2026-08-06)** | `app/Http/Controllers/Web/ReportPageController.php:122-158` | Đường xuất báo cáo chính thức đã thoát công thức CSV, dùng cách xuất tiết kiệm bộ nhớ (stream), và lộ bí mật đã sửa bằng khoá phiên riêng — không còn qua flash message chung. |
| GAP-010b | (Tách từ GAP-010) Đường xuất CSV cũ hơn (`ExportController::generateCsv()`) vẫn đang hoạt động thật, không thoát công thức, dựng toàn bộ file trong bộ nhớ | **RESOLVED (verified 2026-08-09)** | `app/Http/Controllers/Api/ExportController.php:137-174`; route sống tại `routes/api.php:1008-1009` | **Đã vá và xác minh.** Audit gốc không phát hiện vì đường xuất mới (GAP-010a) được thêm song song, không thay thế đường cũ — đường cũ vẫn còn sống và vẫn còn đúng 2 lỗi (chèn công thức + hết bộ nhớ khi file lớn) mà audit tưởng đã vá hết toàn cụm. |
| GAP-010c | (Tách từ GAP-010) Nghi vấn lệch múi giờ Gantt — tìm thấy trang `/schedule` có cách hiển thị ngày tháng khớp kiểu lỗi đã audit | **CLOSED — NOT REPRODUCED (verified 2026-08-10)** | `routes/web.php:934` (`SchedulePageController`); `resources/views/schedule/index.blade.php:112,116` | **Đã điều tra trên baseline xác định, không tái hiện được, không có remediation được claim.** 8 ca kiểm tra trên baseline `1325c0e6`, tất cả `SHIFTED=0`. Không có chuyển đổi múi giờ phía client trên `/schedule`. Schema `tasks.start_date`/`end_date` là `DATE` theo migration. Không được coi là lỗi thật đã xác nhận, không cấp phép sửa. |
| GAP-011 | 21 route `/_debug/*` vẫn tồn tại kể cả login-bypass bằng credential cứng | **OPEN (verified)** cho non-prod / **RESOLVED (verified)** cho prod | `routes/web.php` (18 chỗ có `_debug`), `app/Http/Middleware/DebugGateMiddleware.php:16-24` | `DebugGateMiddleware` chặn 404 ngoài `local/testing/development` — an toàn ở prod. Nhưng vẫn là bề mặt gây nhầm lẫn ở non-prod, và không có test bất biến chống drift (xem GAP-024). |
| GAP-031 | **RESOLVED.** Web document-approval surface (`DocumentController::approve()/reject()`) was dead, unrouted code using a `pending` status and non-fillable audit columns, fully diverged from the real canonical API workflow in `SimpleDocumentController` (`draft→submitted→approved\|rejected`, audit in `metadata` JSON). Additionally, `SimpleDocumentController::update()`/`createVersion()` permitted direct writes of `submitted`/`approved`/`rejected` status, bypassing `document.approve`/`DocumentPolicy::approve()`/`DocumentWorkflowService`/decision audit entirely — a transition-integrity hole discovered during design (rev 3). Fixed via `DocumentWorkflowService` (transactional, `lockForUpdate()`-guarded) as the single mutation owner, consumed by both `SimpleDocumentController` (API) and the new `DocumentWorkflowController` (Web); `document.approve` permission gates all decision actions and the approvals list; `store()`/`update()`/`createVersion()` reject direct writes of the 3 reserved workflow statuses. | **RESOLVED (verified)** | `docs/superpowers/specs/2026-08-04-gap031-document-approval-workflow-design.md` (rev 3, design); `docs/superpowers/plans/2026-08-04-gap031-document-approval-workflow.md` (implementation plan); `app/Services/DocumentWorkflowService.php`, `app/Http/Controllers/Api/SimpleDocumentController.php`, `app/Http/Controllers/Web/DocumentWorkflowController.php` (implementation) | **GAP-031 does not make `Document` eligible for Today Workspace personalized "Action Required"** — that requires a per-document designated approver, which is GAP-033, explicitly out of scope here. See GAP-032 for the broader generic-vs-workflow status semantics investigation (not closed by this gap — only the acute reserved-status bypass is closed). |
| GAP-032 | `Document.status` served two incompatible purposes: "generic status" a client can set to any string via `store()`/`update()` (`active`, `review`, and any other legacy value), and "workflow status" of the `draft/submitted/approved/rejected` state machine. GAP-031 closed the acute bypass (reserved statuses can no longer be written directly), but did not resolve what legacy statuses like `active`/`review` should mean inside the workflow, whether they need a "re-entry" step, or whether legacy values should be normalized/migrated. | **RESOLVED (verified)** | Gate 1/2/3 records: `docs/owner-decisions/GAP-032/01-request.md`, `02-design.md`, `03-release.md` (Gate 3 Owner-approved 2026-08-12, merged to `main` at `19c96cfa6f1b4aca654b280a092b2ff511e749d3` via PR #256); design: `docs/superpowers/specs/2026-08-10-gap032-document-status-semantics-design.md`; plan: `docs/superpowers/plans/2026-08-10-gap032-document-status-semantics.md`; implementation: `app/Enums/DocumentLifecycleStatus.php`, `app/Enums/DocumentApprovalStatus.php`, `app/Models/Support/DocumentStatusResolver.php`, `app/Services/DocumentStatusService.php`, `app/Services/DocumentWorkflowService.php`, `app/Services/DocumentLifecycleService.php`, `app/Services/DocumentVersionService.php`; architecture guard: `tests/Architecture/DocumentMutationOwnershipTest.php` | **Resolved via Option B (Owner Gate 2 decision): two independent dimensions.** Lifecycle (`draft`/`in-review`/`published`/`archived`) and Approval (`not-submitted`/`awaiting-approval`/`approved`/`rejected`) are now separate, explicit, and each has a stable canonical value set — Approval in particular is no longer entangled with generic status editing. Legacy data was not bulk-migrated; untouched legacy rows keep their original values (nullable canonical columns, deterministic legacy-projection fallback). **This gives GAP-033 a stable, unambiguous `awaiting-approval` Approval state to reference** — see GAP-033's row below, now updated to build on this. |
| GAP-034 | Đường xuất CSV task/project (`ExportController::exportTasks()`/`exportProjects()`) xây câu truy vấn `Task::with(...)->get()`/`Project::with(...)->get()` không lọc theo `tenant_id` — `Task`/`Project` không có global scope theo tenant, `Project::scopeForTenant()` là scope tự chọn mà controller không gọi | **RESOLVED (verified 2026-08-09)** | `app/Http/Controllers/Api/ExportController.php:16-132`; `src/CoreProject/Models/Task.php`, `src/CoreProject/Models/Project.php` (không có `addGlobalScope`); `Project.php:242` (`scopeForTenant`, opt-in, không được gọi); Gate 1: `docs/owner-decisions/GAP-034/01-request.md` | **Đã vá và xác minh.** Phát hiện trong lúc chuẩn bị Gate 2 của GAP-010b — tách thành work item riêng theo chỉ đạo owner (2026-08-06/07): không được gộp chìm vào GAP-010b, phải là điều kiện bắt buộc trước khi 2 route xuất dữ liệu được khôi phục hoạt động trở lại. Có thể triển khai cùng đợt/PR với GAP-010b nhưng phạm vi/tiêu chí/kiểm thử/bằng chứng/quyết định owner phải tách biệt cho từng gap. |
| GAP-033 | `Document` has no mechanism to designate a specific approver/action-owner per document — `document.approve` is a tenant/role-wide permission ("anyone with this permission in the tenant"), not "this exact person is responsible for deciding on this specific document." This is the reason Document approval was excluded from Today Workspace's personalized "Action Required" section (`docs/superpowers/specs/2026-07-31-today-workspace-mvp-design.md` §7 — that section requires an actor identifiable by a specific, individual query condition, which a tenant-wide permission cannot provide). Neither GAP-031 (workflow correctness/audit) nor GAP-032 (Lifecycle/Approval dimension split) added a per-document approver field — both explicitly left this out of scope. | **RESOLVED (verified 2026-08-13)** | `app/Policies/DocumentPolicy.php:113-121` (`approve()` checks only tenant match + role-wide `document.approve` permission, no per-record actor comparison); `app/Models/Document.php:102-132` (`$fillable` has no `approver_id`/`assigned_to`/`reviewer_id`); `app/Models/DocumentApprovalEvent.php:53-63,116-120` (`actor_id` is a post-hoc audit field recorded at decision time, not a pre-assignment field); `docs/superpowers/specs/2026-07-31-today-workspace-mvp-design.md` §7 (line 316, admission condition 1: "Actor xác định được bằng 1 điều kiện truy vấn cụ thể"); `docs/superpowers/specs/2026-08-04-gap031-document-approval-workflow-design.md` §16; **Gate 1/2/3 records:** `docs/owner-decisions/GAP-033/01-request.md`, `02-design.md`, `02-design-v2.md`, `03-release.md` (Gate 3 Owner-approved 2026-08-13, merged to `main` at `30a609a9390524f3294a2eb579141f7d013064fb`); implementation: `app/Models/DocumentApproverAssignment.php`, `app/Services/DocumentApproverAssignmentService.php` | **Re-verified 2026-08-12 after GAP-032 Gate 3 release** — GAP-032 gives GAP-033 a stable canonical `awaiting-approval` Approval state to key off of, but did not add an approver field; the gap is unchanged. **Existing repo precedent for a per-record actor field enforced at the policy layer:** `Ncr` model already has this exact pattern — `assigned_to` column (`database/migrations/2025_09_20_142033_create_ncrs_table.php:27`) + `NcrPolicy::resolve()` checking `$user->id === $ncr->assigned_to || $user->hasRole([...])` (`app/Policies/NcrPolicy.php:47-51`). `Rfi` has an `assigned_to` column too (`database/migrations/2025_09_20_133629_create_rfis_table.php:30`) but its policy does NOT check it for `approve()` (`app/Policies/RfiPolicy.php:47-51`, role-only — same class of gap as Document, tracked separately as GAP-030). **RESOLVED via GAP-033 Gate 3 (2026-08-13, PR #258):** adds a per-document designated approver defaulting to the project manager, overridable per document; assignment restricted to the project's own manager or a tenant Admin; target must be same-tenant, active project-team member, and independently hold `document.approve`; assignment never grants approval authority by itself; every change append-only audited. `DocumentPolicy::approve()` and all GAP-032 core services unmodified (verified by diff review at Gate 3). Reconciled 2026-08-13 under OWN-2026-008 — see `docs/owner-decisions/OWN-2026-008/` (register row was stale relative to already-merged Gate 3 state). |
| GAP-030 | `POST /api/zena/rfis/{id}/resolve-escalation` gate bằng `rbac:rfi.escalate` — permission này đúng ra chỉ nên gate ai được TẠO escalation, không phải ai được RESOLVE nó. Escalation target hợp lệ (`escalated_to`) mà không giữ `rfi.escalate` sẽ bị middleware chặn ở tầng route trước khi chạm tới `RfiController::resolveEscalation()`'s target/PM/admin check (vốn đã đúng và có test) | **OPEN (verified) — intentionally deferred** | `routes/api_zena.php:288`, `app/Http/Controllers/Api/RfiController.php:513-567` (logic 3-way target/PM/admin đã đúng, chỉ là route middleware chặn trước khi tới được đó); phát hiện lại lúc đóng gap này qua `tests/Feature/Api/RfiApiTest.php::test_escalation_target_can_resolve_their_own_escalation` (test hiện tại phải tự cấp `rfi.escalate` cho target mới pass — dấu hiệu rõ của gap) | **Không phải không sửa được về kỹ thuật — cố tình hoãn vì cần quyết định capability model trước.** Đã thử gỡ hẳn `rbac:` khỏi route (giải pháp sai): vi phạm 2 test bất biến bảo mật toàn repo (`RouteMiddlewareSecurityContractTest`, `ZenaRouteSurfaceInvariantTest` — bắt buộc MỌI route đã `auth:sanctum` phải có `rbac:<permission>` tĩnh, không có cơ chế ngoại lệ cho `ZenaRouteSurfaceInvariantTest`). **Hướng kiến trúc đã chốt cho lần sửa sau:** (1) route vẫn giữ middleware tĩnh nhưng đổi sang permission riêng `rbac:rfi.resolve-escalation` (tách khỏi `rfi.escalate`); (2) permission tĩnh này kiểm tra trên **actor** (người gọi request), không phải trên target; (3) policy record-level trong controller tiếp tục kiểm tra động theo RFI/project/tenant/trạng thái workflow/quan hệ actor-với-record/tính hợp lệ của target — không đổi; (4) việc bất kỳ role nào cũng CÓ THỂ trở thành target không có nghĩa role đó phải được seed permission — chỉ role được phép hành động với tư cách **actor** (gọi resolve) mới cần `rfi.resolve-escalation`; (5) **không tự suy đoán danh sách role nào là actor hợp lệ trong lần sửa tiếp theo** — cần xác nhận ma trận actor-role từ business trước khi implement, tránh lặp lại rủi ro breaking-vs-seeding đã gặp ở lần đóng gap này. |

## Tier 3 — Workflow cốt lõi tạo doanh thu / thu tiền

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-012 | Change Request `apply()` không gửi bất kỳ thông báo nào cho bên liên quan | OPEN (tự khai trong backlog) | `docs/roadmap/backlog.yaml:296` (S3.2a) | Story đã đánh dấu `done` nhưng cố tình chừa gap này — người phê duyệt/PM không biết CR đã áp dụng trừ khi tự vào xem. Đây là workflow dead-end đúng nghĩa mục 4 hiến pháp. |
| GAP-013 | Submittal (vật tư) không có fan-out thông báo khi submit/review/approve/reject | OPEN (tự khai trong backlog) | `docs/roadmap/backlog.yaml:388` (S4.3) | Ảnh hưởng trực tiếp tốc độ phê duyệt vật tư → tốc độ thi công/thu tiền theo mốc. |
| GAP-029 | Operator web UI chưa có đường "mở lại để sửa"/nộp lại (`revising` → resubmit) cho Submittal bị reject — backend/API đã đủ, UI chưa nối | **RESOLVED (verified 2026-08-13)** | `resources/views/submittals/show.blade.php:93` chỉ có nhánh `status === 'draft'`, không có nhánh `rejected`/`revising`; `app/Http/Controllers/Web/SubmittalPageController.php:122-168` có `submit/approve/reject` nhưng không có `startRevision`, không proxy `POST /submittals/{id}/start-revision` (route đã có ở API, xem `routes/api_zena.php`); **RESOLVED via PR #230 (`d6ca498b feat(submittal): GAP-029 — operator web UI for resubmit flow`)** — `show.blade.php` nay có đủ nhánh `rejected`/`revising` và nút "Sửa lại" gọi `POST operator.submittals.start-revision` | PR#229 (2026-07-24/25) đóng ngõ cụt state machine ở tầng API (`SubmittalLifecycleService`, bảng `submittal_revisions`, `SubmittalPolicy`) nhưng CỐ Ý không nối UI ngay lúc đó — quyết định scope từ lúc brainstorm. PR#230 (2026-07-25) sau đó nối UI đúng như đề xuất: nút "Sửa lại" ở `show.blade.php` khi `status===rejected`/`revising` → gọi `POST .../start-revision`, theo đúng contract đã có ở `SubmittalController::startRevision()`/`submit()`. Liên quan GAP-013 (cùng entity Submittal, khác khía cạnh — GAP-013 là thiếu thông báo, vẫn OPEN, không đổi bởi reconciliation này). Reconciled 2026-08-13 dưới OWN-2026-008 — dòng này trước đó vẫn ghi OPEN dù đã merged. |

## Tier 4 — Tiến độ, trách nhiệm, cảnh báo

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-014 | NCR/CAPA: dashboard và ngữ nghĩa thông báo còn deferred; liên kết NCR↔task lưu trữ lâu dài còn UNKNOWN | **PARTIALLY RESOLVED (verified)** | `docs/roadmap/backlog.yaml:461` (S5.2) | *Trạng thái trước Wave 1: `UNVERIFIED (một phần có thể đã đóng)`.* S5.3 (escalation rules) đã `done` (2026-07-07) nên phần escalation có thể đã xong. **Xác minh Wave 1 (2026-08-06):** xác minh riêng từng phần cho kết quả KHÁC NHAU. Xem 3 dòng con GAP-014a/GAP-014b/GAP-014c ngay bên dưới — các dòng con là nơi có thẩm quyền để hành động. |
| GAP-014a | (Tách từ GAP-014) Bảng điều khiển (dashboard) NCR/CAPA | **RESOLVED (verified 2026-08-06)** | `app/Http/Controllers/Api/SiteEngineerDashboardController.php:80,101-160` | Bảng điều khiển tính số liệu NCR/CAPA thật, đúng theo từng công ty (tenant), từ dữ liệu sống. |
| GAP-014b | (Tách từ GAP-014) Ngữ nghĩa thông báo NCR — sự kiện chưa từng được kích hoạt | **OPEN (verified 2026-08-06)** | `app/Listeners/NcrEventListener.php`; không có nơi nào gọi `NcrCreated`/`NcrAssigned`/`NcrResolved`; không đăng ký trong `app/Providers/EventServiceProvider.php` | Mã gửi thông báo NCR đã viết đầy đủ (giống như đã lắp chuông báo) nhưng chưa từng được kích hoạt (chưa nối dây điện) — không có NCR nào thật sự kích hoạt việc gửi thông báo. Đường thông báo NCR không hoạt động thật. |
| GAP-014c | (Tách từ GAP-014) Liên kết lâu dài NCR ↔ công việc khắc phục (CAPA task) | **OPEN (verified 2026-08-06)** | `database/migrations/2025_09_20_142033_create_ncrs_table.php` (không có cột `task_id`/`capa_task_id`); `app/Models/Ncr.php` (không có quan hệ task) | Không có cột hay quan hệ nào lưu liên kết lâu dài giữa NCR và công việc khắc phục — chỉ liên kết tạm qua tag, không bền vững. Chưa có lưu trữ liên kết bền. |
| GAP-015 | Không có UI/screen owner đã xác nhận cho toàn bộ engine WorkTemplate → WorkInstance (được audit gọi là "hệ con mạnh nhất" của hệ thống) | OPEN (tự khai là ngoài phạm vi MVP) | `docs/audits/2026-04-04-s6-4-project-template-walkthrough-coverage.md:119-121`, `docs/roadmap/backlog.yaml:553-556` (S6.4) | Backend/API/DB đã chứng minh hoạt động nhưng người dùng thực tế không có màn hình để dùng — dữ liệu được lưu nhưng không tạo ra quyết định quản trị (đúng dấu hiệu mục 4). Đây là gap **chiến lược**, không phải bug — cần quyết định business trước khi làm UI, không tự ý code thêm. |

## Tier 5 — Chi phí, lợi nhuận, đối soát

*(Không có gap riêng biệt ngoài GAP-003 ở Tier 1, vốn cũng ảnh hưởng đối soát vật tư/procurement — xem ghi chú tại đó. Không tìm thấy gap thuần cost/profit trong các audit hiện có; đây là điểm mù cần audit riêng nếu ưu tiên goal #2/#5 của ZENA vision.)*

## Tier 6 — Trải nghiệm người dùng (route/view chết hoặc gây nhầm lẫn)

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-016 | Route `/admin-dashboard-enhanced` và `/projects-enhanced` trỏ tới view không tồn tại | **OPEN (verified)** | `routes/web.php:227-234`; view thiếu tại `resources/views/admin/dashboard-enhanced.blade.php`, `resources/views/app/projects-enhanced.blade.php` | Người dùng chạm route này sẽ gặp lỗi view-not-found. Đề xuất: xoá route (delete-candidate theo audit gốc). |
| GAP-017 | Link "invitation expired" trỏ view `invitations.expired` không tồn tại | **OPEN (verified)** | `app/Http/Controllers/Web/InvitationController.php:152`; `resources/views/invitations/` chỉ có `accept/create/manage/index.blade.php` | Người dùng bấm link mời hết hạn sẽ gặp lỗi 500 thay vì trang thông báo hợp lý — real user-facing dead-end, ưu tiên cao hơn các gap UX khác vì chạm trực tiếp workflow mời thành viên. |
| GAP-018 | 4 component "smart tools" (`smart-search`, `smart-filters`, `analysis-drawer`, `export-component`) + `test-smart-tools.blade.php` gọi sai prefix API (`/api/universal-frame/*` thay vì `/api/v1/universal-frame/*`), một số endpoint không tồn tại luôn | **OPEN (verified)** | Cả 5 file còn tồn tại tại `resources/views/components/*` và `resources/views/test-smart-tools.blade.php` | Không route nào mount tới các file này — an toàn nhưng là rác kiến trúc, dễ gây nhầm cho người sửa sau. Đề xuất: archive/xoá. |
| GAP-019 | `layouts/universal-frame.blade.php` (shell đầy đủ 8 component) không có route owner chính danh nào | **OPEN (verified)** | File còn tồn tại `resources/views/layouts/universal-frame.blade.php` | Đã có quyết định "giữ làm debug/demo shell, không hồi sinh nếu chưa có vòng xác định owner" — hợp lệ, giữ nguyên trạng thái này. |
| GAP-020 | `admin/users/index.blade.php` và `admin/tenants/index.blade.php` là file mồ côi — route thật render file khác (`admin.users`, `admin.tenants`) | **OPEN (verified)** | Cả hai file còn tồn tại `resources/views/admin/{users,tenants}/index.blade.php` | Rủi ro cao: tên file theo đúng convention Laravel nên người sửa sau dễ tưởng đây là file đang chạy thật. Đề xuất: archive kèm comment rõ, hoặc xoá. |
| GAP-021 | `/api/v1/tasks*` (compatibility) và `/api/zena/tasks*` (canonical) có 3 khác biệt: envelope response, middleware (`input.sanitization`+`error.envelope` thiếu ở compatibility), tên permission RBAC (`task.update` vs `task.edit`) | UNVERIFIED (audit tự nhận "contributor confusion: high") | `docs/audits/2026-03-19-tasks-contract-parity-audit.md:176-183`, `docs/audits/2026-03-19-tasks-v1-split-owner-route-inventory-audit.md:150-159` | 4 controller owner khác nhau cho cùng một khái niệm "task assignment" — nợ kỹ thuật thật, không phải lỗi tenant/security. Không khẩn nhưng nên gộp về 1 owner khi có dịp chạm vào task API. |

*(GAP-022, GAP-023, GAP-024 dưới đây đã xác nhận **RESOLVED** — giữ lại làm lịch sử theo mục 8, không cần hành động.)*

| ID | Tiêu đề | Status | Bằng chứng |
|---|---|---|---|
| GAP-022 | `/templates*` từng trỏ view không tồn tại | **RESOLVED (verified)** | `routes/web.php:433-435` có comment "Route /templates* đã gỡ 22/07: trang demo giả hoàn toàn chết" — khớp PR#218/#219 |
| GAP-023 | 3 route settings con (`general`/`security`/`notifications`) từng trỏ view không tồn tại | **RESOLVED (verified)** | `routes/web.php:441-449` nay `redirect()->route('app.settings')` thay vì render view chết |
| GAP-024 | `/app/projects/{project}` từng trả JSON thay vì HTML dù có view Blade sẵn | **RESOLVED (verified)** | `app/Http/Controllers/Web/ProjectController.php:139` nay khai báo `: View`, không còn `JsonResponse` |
| GAP-025 | 3 file demo tĩnh công khai (`api-demo.html`, `logo-test.html`, `projects-dashboard-test.html`) | **RESOLVED (verified)** | Cả 3 file xác nhận không còn tồn tại trong `public/` |

## Tier 7 — Tự động hoá & tối ưu nâng cao

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-026 | `production.yml` vẫn dùng `secrets.SLACK_WEBHOOK_URL` không rõ kênh đích, trong khi 2 workflow khác đã chuyển sang secret theo kênh riêng | **BLOCKED (external)** | `.github/workflows/production.yml:153-182` vs `.github/workflows/automated-deployment.yml:26,125-682`, `.github/workflows/release-management.yml:29,302-386` | Không thể đóng chỉ bằng code — cần người quản trị Slack xác nhận secret hiện trỏ kênh nào trước khi sửa `production.yml`, nếu không có nguy cơ đổi route cảnh báo prod mà không ai biết. Nguồn thẩm quyền nhất trong 3 audit Slack: `docs/audits/slack-routing-secret-inventory.md` (2 file kia đã cũ hơn/stale). |
| GAP-027 | Không có test bất biến so khớp route `/_debug/*` đang mount với inventory snapshot | **RESOLVED (verified 2026-08-13)** | `docs/audits/2026-03-19-debug-route-inventory.md:66,71`; **RESOLVED:** `tests/Feature/DebugRouteDocumentationInvariantTest.php` — so khớp claim active/archived `_debug/*` với route thật đang mount qua `route:list` | Ưu tiên thấp; liên quan GAP-011 (vẫn OPEN, không đổi bởi reconciliation này). Reconciled 2026-08-13 dưới OWN-2026-008 — test bất biến được yêu cầu đã tồn tại sẵn trên `origin/main`, dòng này trước đó vẫn ghi UNVERIFIED. |

## Tier 8 — Trang trí / giá trị vận hành thấp

| ID | Tiêu đề | Status | Bằng chứng | Ghi chú / hành động đề xuất |
|---|---|---|---|---|
| GAP-028 | `README.md` và `SYSTEM_DOCUMENTATION.md` mô tả kiến trúc đã lỗi thời (Vue 3, microservices, universal-frame là UI chính) | **RESOLVED (verified 2026-08-13)** | `docs/audits/2026-03-19-system-review-roadmap-repair.md:126,128,143-146`; **RESOLVED:** `README.md` hiện tại không còn nhắc "Vue"/"microservice" (grep xác nhận 0 kết quả); `SYSTEM_DOCUMENTATION.md` không còn tồn tại ở gốc repo, chỉ còn bản archive tại `docs/archive/reports/SYSTEM_DOCUMENTATION.md` | Rủi ro: gây hiểu sai cho người mới. Reconciled 2026-08-13 dưới OWN-2026-008 — README đã được viết lại (commit `f9eaea0f` "Docs: codify routing architecture rules") và SYSTEM_DOCUMENTATION.md đã archive, dòng này trước đó vẫn ghi UNVERIFIED. |

---

## Đã hợp nhất từ (nguồn)

`docs/audits/2026-03-19-blade-ownership-reachability-audit.md`, `2026-03-19-debug-route-inventory.md`, `2026-03-19-legacy-surface-rationalization-backlog.md`, `2026-03-19-public-demo-artifact-audit.md`, `2026-03-19-system-review-roadmap-repair.md`, `2026-03-19-tasks-contract-parity-audit.md`, `2026-03-19-tasks-v1-mounted-source-drift-triage.md`, `2026-03-19-tasks-v1-split-owner-route-inventory-audit.md`, `2026-03-19-universal-frame-ownership-audit.md`, `2026-04-04-s6-4-project-template-walkthrough-coverage.md`, `2026-07-09-acceptance-review.md`, `production-slack-webhook-evidence.md`, `slack-notify-step-inventory.md` (lưu ý: chính audit này tự nhận đã stale so với bản dưới), `slack-routing-secret-inventory.md`, và `docs/roadmap/backlog.yaml`.

## Chưa làm trong lần hợp nhất này

- Đã verify lại 9 mục UNVERIFIED liên quan bảo mật trong Wave 1 (2026-08-06) — xem OWN-2026-002/OWN-2026-003; GAP-010 được tách thành ba dòng con với trạng thái riêng.
- Chưa có audit riêng cho Tier 5 (cost/profit/reconciliation) — khoảng trống nhận thức, không phải kết luận "không có gap".
- Chưa hành động sửa bất kỳ mục nào — đúng tinh thần mục 4 hiến pháp (ghi nhận trước, không tự ý sửa).

## Cách cập nhật register này

Khi một gap được sửa: đổi `Status` thành `RESOLVED (verified)` kèm PR/commit liên quan, không xoá dòng — giữ làm lịch sử theo mục 8 hiến pháp. Khi phát hiện gap mới: thêm dòng mới đúng tier, có bằng chứng file:line, không suy đoán.
