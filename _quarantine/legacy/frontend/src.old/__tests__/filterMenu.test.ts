import { describe, it } from 'vitest';

describe('filterMenu (legacy coverage)', () => {
  it.todo('filters items by roles and tenants with real data fixtures');
  it.todo('supports nested children filtering with RBAC');
  it.todo('aligns permission codes with menu ids (menu.<id>.view)');
});
