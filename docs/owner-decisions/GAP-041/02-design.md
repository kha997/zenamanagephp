---
work_id: GAP-041
gate: 2
gate_status: approved
owner_decision:
  value: approve
  authority: human_owner
decision_requested: "approve_or_changes_or_decline"
references:
  spec: docs/superpowers/specs/2026-08-21-gap-041-ci-test-selection-truthfulness-design.md
  plan: null
  branch: docs/GAP-041-gate2-design
  pr: 277
  release: null
decision_provenance:
  trust_level: claimed_repo_record
  recorded_by: agent
  recorded_at: "2026-08-21T18:10:00+07:00"
  owner_response_reference: "Owner Gate 2 decision — APPROVE v3, recorded against PR #277 head feb174a9c4c5785ac60930dfc6545a95771d140b (Gate 1 approved record: PR #276 commit 74635722e7465ff043a257c78d6de040f5bf85c5): 'GAP-041 — GATE 2 OWNER DECISION: APPROVE. I APPROVE GAP-041 Gate 2 v3. The approved target is Option D. The approved acceptance contract is binding: GAP-041 is complete only when the real performance-tests surface truthfully executes its intended performance-test population on real MySQL and fails non-zero if that intended population unexpectedly selects zero tests; the never-implemented performance-budget and performance-heavy tier claims are retired completely, including their consequential test-summary references; and every required LIVE criterion is empirically demonstrated on the exact implementation tree before Gate 3 is presented as complete. There is no Gate-3-complete-with-LIVE-pending state. Implementation is now authorized only within the approved Gate-2 boundary. Merge, release and deployment remain NOT authorized. Binding clarifications: Option A is withdrawn; no new performance_budget/performance_heavy tests are authorized under GAP-041; test-summary cleanup is part of the approved implementation surface; no separate missing-script GAP is currently warranted; LIVE requirements cannot be waived.' PRIOR — Owner Gate 2 decision — REQUEST CHANGES (v2 -> v3), recorded against PR #277 head 56d5df17be7633e85530f837ab091f4abf1fdce1 (Gate 1 approved record: PR #276 commit 74635722e7465ff043a257c78d6de040f5bf85c5): 'GAP-041 — GATE 2 OWNER DECISION: REQUEST CHANGES — v2 -> v3 ... The corrected Option D direction and hard-LIVE acceptance model are accepted in principle. However, exact-baseline review found additional current-repository facts that must be reconciled before Gate 2 can be approved. NO implementation is authorized. FINDING 1 — test-summary IS A REAL CONSUMER OF THE TWO JOBS: the v2 engineering design states no workflow consumer/downstream process depends on performance-budget/performance-heavy; that is false on the reviewed canonical baseline — a11y-perf-testing.yml's test-summary job has needs: [accessibility-tests, performance-budget, performance-heavy, lighthouse-ci, e2e-tests] and its generated summary body emits Performance Budget Tests / ${{ needs.performance-budget.result }} and Performance Heavy Tests / ${{ needs.performance-heavy.result }}. Option D cannot be implemented safely by deleting only the two job definitions — expand Option D's implementation surface to include the minimum consequential cleanup: delete performance-budget, delete performance-heavy, remove both job IDs from test-summary.needs, remove the corresponding Budget/Heavy summary sections and needs.* references, remove any other stale references proven by exhaustive search. Distinguish a true downstream dependency (requires preserving a job) from a reporting/reference consumer that must simply be updated when a job is intentionally retired — test-summary is the latter. Do not broaden into unrelated workflow cleanup. FINDING 2 — RECHECK THE MISSING-SCRIPT BLAST RADIUS FROM EXACT CURRENT CODE: the v2 design/report's claim that the missing ci_prepare_testing_env.sh continues to break accessibility/E2E/Lighthouse after the two phantom jobs are retired does not match the currently reviewed workflow — accessibility-tests, lighthouse-ci, and e2e-tests each perform their own explicit environment setup; the only direct calls to ./.github/scripts/ci_prepare_testing_env.sh are in the phantom performance jobs. Perform an exhaustive exact-baseline repo-wide search for ci_prepare_testing_env.sh and classify every reference, then correct the disposition based on facts. If the only live references are inside performance-budget/performance-heavy, Option D removes those references as part of retirement — do NOT recommend a separate missing-script Operational Gap merely because the file doesn't exist, an unused nonexistent file after its only consumers are removed is not itself an actionable runtime defect, explicitly state the former blocker disappears as a consequence of retiring its only consumers, and do not describe accessibility/E2E/Lighthouse failures as caused by that script unless exact code evidence proves the causal relationship. If other live references exist elsewhere, enumerate them exactly by file/job/step and retain the separate-gap recommendation based only on those verified consumers — no inference from a workflow run where several unrelated jobs happened to fail together. GATE-1 EPISTEMIC CORRECTION: do not reopen or invalidate the approved core GAP-041 Gate-1 decision — the core finding remains valid (performance-tests = LIVE zero-test false-green; performance-budget/performance-heavy = nonexistent selected populations/phantom tier semantics) — but if Gate-1 audit section 1.4 overstated the missing-script blast radius, record a narrow epistemic correction in the Gate-2 v3 engineering design stating the out-of-scope missing-script blast-radius statement from Gate 1 was broader than current exact-baseline code supports, and this does not alter the approved GAP-041 core problem statement; do not silently perpetuate a known-inaccurate side claim merely because Gate 1 was already approved; do not create a new artifact solely for this erratum unless repository governance actually requires one. FINDING 3 — PR #277 BODY IS STILL V1: the current PR body still presents 3 options and recommends Option A, stale and contradicting the v2 packet/spec; update PR #277 description as metadata only (first non-empty line remains exactly Work ID: GAP-041; describe Option D as the current recommendation; say Option A is withdrawn; mention four evaluated approaches A-D; describe the hard/non-waivable LIVE acceptance contract; reflect the corrected treatment of test-summary; reflect the verified missing-script disposition after the exhaustive search; continue stating no implementation is authorized) — PR-body correction is metadata only, need not create a new commit. REVISED OPTION D SEMANTICS: surviving real CI surface automated-testing.yml -> performance-tests (retain the file-matrix job, restore selection of its existing @group performance tests, use --fail-on-empty-test-suite or a technically equivalent native fail-closed mechanism, preserve genuine MySQL preflight); retired phantom tier surfaces a11y-perf-testing.yml (remove performance-budget, remove performance-heavy, remove their direct reporting/dependency references from test-summary, remove only directly-associated stale tier claims/artifact references proven by exact search, preserve accessibility/Lighthouse/E2E semantics otherwise); do not invent real budget/heavy tests under GAP-041, real budget/heavy tiers if desired later require a separately-scoped design with actual performance semantics. REVISED GATE-3 ACCEPTANCE CONTRACT under Option D: (A) phantom-tier retirement proof — prove statically from the exact committed workflow that no performance-budget job definition remains, no performance-heavy job definition remains, test-summary.needs does not reference either, generated Test Summary does not claim/report either, no dangling needs.performance-budget or needs.performance-heavy expression remains, no stale tier-specific artifact reference remains where its producer has been retired, the resulting workflow must remain syntactically and structurally valid, use the repository's available workflow/YAML linting mechanism and where possible a real GitHub Actions dispatch as final evidence; (B) surviving performance-tests LIVE proof, mandatory and non-waivable — name/claim matches actual coverage, intended population traceable to actual matrix-selected performance files, at least one intended test executes on each applicable matrix leg in a LIVE GitHub Actions run on the exact implementation tree, real-MySQL fail-closed preflight succeeds before the tests, zero-selection causes non-zero failure, fail-closed behavior LIVE-proven using a disposable proof branch/commit or equivalent isolated validation method then removed/reverted cleanly, no STATIC/LOCAL result promoted to LIVE; there is no Gate-3-complete-with-LIVE-pending state. IMPLEMENTATION-MECHANISM CHECK FOR LATER: Gate 2 may still recommend native --fail-on-empty-test-suite, but the future implementation plan must first verify how the repository's actual php artisan test wrapper exposes/passes that PHPUnit 11 option — the Gate-2 contract is behavioral (zero intended tests must cause non-zero failure), do not make Gate 3 depend blindly on a CLI spelling not yet verified through the actual Laravel/Collision wrapper used by performance-tests; if the wrapper cannot pass the option correctly, implementation may use the smallest technically equivalent native PHPUnit invocation that preserves the approved file-matrix semantics; this verification belongs in the future implementation plan, not Gate-2 code. SCOPE/STOP: still prohibited in this v3 correction — workflow edits, test edits, application edits, implementation plan, GAP-042 work, unrelated CI repair, merge, release, deployment; only revise docs/owner-decisions/GAP-041/02-design.md, the Gate-2 engineering spec, and PR #277 body metadata; keep gate_status: awaiting_owner and owner_decision.value: none once revised; run exact-head Owner Governance Lint, --enforce-gate-ordering, and Routes Guardrails; then STOP and report new Gate-2 head SHA, exact files changed, corrected test-summary dependency analysis, exhaustive missing-script reference inventory, whether a separate missing-script GAP is still actually warranted, any Gate-1 side-claim erratum recorded, final Option D implementation surface, revised Gate-3 acceptance contract, confirmation LIVE is still non-waivable, PR-body correction status, exact-head CI results, and confirmation no implementation/GAP-042/unrelated repair occurred. Wait for a new Owner Gate-2 decision.'"
  reconciliation_required: false
supersedes: null
superseded_by: null
timestamps:
  created_at: "2026-08-21T15:45:00+07:00"
  updated_at: "2026-08-21T17:20:00+07:00"
generated_by: agent
---

## Revision log

**Round 1 — Owner chose: Request changes**, against PR #277 head `372a2d97fcb083a46e331be35d2a5430892c0796` (2026-08-21). Summary: Option A's "relabel `performance-budget`/`performance-heavy` to `--group performance`" did not establish semantic truthfulness (it relocates the false claim rather than removing it); Owner directed adding Option D (repair `performance-tests`, retire the two phantom tier jobs), removing the Gate-3 LIVE/STATIC-pending escape hatch, and reconciling `references.spec` against the GAP-040 Gate-2 convention. v2 addressed all of the above.

**Round 2 — Owner chose: Request changes**, against PR #277 head `56d5df17be7633e85530f837ab091f4abf1fdce1` (2026-08-21). Verbatim decision in `decision_provenance.owner_response_reference` above. Option D and the hard-LIVE model were accepted in principle, but exact-baseline review found: (1) v2's claim that no workflow consumer depends on `performance-budget`/`performance-heavy` was **false** — `test-summary`'s `needs:` list and generated summary body genuinely reference both; Option D must include their consequential cleanup, not just job deletion; (2) v2's missing-script blast-radius claim (that it also blocks `accessibility-tests`/`e2e-tests`/`lighthouse-ci`) did not match exact current code and required an exhaustive re-check; (3) PR #277's body was still v1's (recommending Option A). **This v3 corrects all three** — see the engineering spec §1.3, §3, §6, §9 for full reasoning; this packet's recommendation, acceptance contract, and scope below are updated accordingly.

**Round 3 — Owner chose: Approve**, against PR #277 head `feb174a9c4c5785ac60930dfc6545a95771d140b` (2026-08-21, Gate 1 approved record PR #276 commit `74635722e7465ff043a257c78d6de040f5bf85c5`). Verbatim decision in `decision_provenance.owner_response_reference` above. Gate 2 v3 is **APPROVED as written**: Option D is mandatory, Option A remains withdrawn, no new `performance_budget`/`performance_heavy` tests are authorized under GAP-041, the `test-summary` consequential cleanup is part of the approved implementation surface, no separate missing-script GAP is currently warranted, and every LIVE Gate-3 criterion in the acceptance contract above is binding and non-waivable — there is no Gate-3-complete-with-LIVE-pending state. Implementation is authorized strictly within the Gate-2 boundary defined above (§ Scope boundaries). Merge, release, and deployment remain **not authorized** pending a separate Gate-3 Owner decision.

## Gate 1 provenance

Gate 1 approved by Owner on `docs/owner-decisions/GAP-041/01-request.md` at commit `74635722e7465ff043a257c78d6de040f5bf85c5` on PR #276 (reviewed head `98a45eac9d2819caa0ea40c572f2a0ac8d675aa3`, canonical baseline `0b77747551a3e0da08e3e41c73a0a88f529b19f3`). PR #276's CI is green at the Gate-1-approval commit. This Gate 2 packet is a separate Draft PR (#277) cut from clean `origin/main` at the same baseline, per the GAP-039/GAP-040 gate-ordering precedent — Gate 1 history is not mixed into this branch.

## `references.spec` reconciliation (per Owner's finding)

v1 pointed `references.spec` at the carried-forward Gate-1 audit doc, which only satisfied the governance-lint dangling-link check without matching the repository's actual convention. Checked against `docs/owner-decisions/GAP-040/02-design.md`'s frontmatter: at Gate 2, `references.spec` is expected to identify the **Gate-2 engineering design** (a `docs/superpowers/specs/*-design.md` file), not the Gate-1 evidence file — Gate 1's own audit stays cited by Gate 1's own packet. Corrected: `references.spec` now points to `docs/superpowers/specs/2026-08-21-gap-041-ci-test-selection-truthfulness-design.md`, a new engineering-design document holding the full reconstruction, current-state matrix, options A–D comparison, and evidence classification. This `02-design.md` is now the shorter Owner-facing decision packet, matching the GAP-040 split between decision surface and engineering evidence.

The v1 carried-forward copy of the Gate-1 audit doc (`docs/audits/2026-08-21-gap-041-zero-test-performance-ci-evidence.md`) is now unreferenced by any frontmatter on this branch — nothing in this packet or the new spec points to it — and has been **removed**, since it was an unnecessary duplicate (it never existed on `origin/main`; the canonical copy lives on Gate 1's own PR #276). One governance subtlety verified before removing it: `docs/audits/**` is not one of `owner_governance_lint.php`'s `OWNER_GOVERNANCE_DESIGN_ONLY_PATH_PREFIXES` (only `docs/owner-decisions/`, `docs/superpowers/specs/`, `docs/superpowers/plans/` qualify), so if this PR's final diff still touched anything under `docs/audits/` — including a same-PR add-then-remove that left history but no net path change — it would matter only if the changed-files computation were commit-by-commit. It is not: both the local `--changed-files-json` exemption check and (per `scripts/ci/fetch-pr-changed-files.sh`'s use of the GitHub PR-files API) CI's own check are tree-diff-based against `origin/main`, so a file added and then removed within the same PR, ending in a state identical to the base, drops out of the changed-file set entirely — verified locally by constructing the exact changed-file list post-removal and re-running `--enforce-gate-ordering`, which now passes. No governance gap was tripped; the earlier v2 draft of this note incorrectly assumed the opposite and has been corrected here.

## Owner Summary

GAP-041 is a CI truthfulness/test-selection integrity defect. `performance-tests` is live-proven to select zero tests while still reporting `success`. `performance-budget`/`performance-heavy` are proven (static + local) to have the same zero-selection defect, but are additionally revealed — per Owner's round-1 finding — to be **phantom job claims**: no test, ever, in this repository's history has implemented the `performance_budget`/`performance_heavy` semantics those two jobs claim to verify, and their `PERF_ITERATIONS`/`PERF_SEED_COUNT` env vars are never consumed anywhere. Simply repointing their PHPUnit `--group` selector at the generic `performance` group (v1's Option A) does not fix this — it only trades "zero tests under a claimed tier" for "generic tests under a tier claim those tests don't implement." The corrected recommendation is **Option D**: repair `performance-tests`' real selector defect and *retire* the two phantom tier jobs outright, rather than relabel or backfill them under this gap.

## Recommended option: D (repair the real surface, retire the phantom tiers — implementation surface expanded in v3)

- **`performance-tests`**: add `--group performance` to restore its real, always-intended file-matrix population; add `--fail-on-empty-test-suite` or a technically equivalent native fail-closed mechanism (see the implementation-mechanism note below); preserve the existing real-MySQL fail-closed preflight unchanged.
- **`performance-budget` / `performance-heavy`**: delete these two job definitions from `a11y-perf-testing.yml`. No fake `@group` annotations are created, no new budget/heavy tests are invented under GAP-041, and the unused `PERF_ITERATIONS`/`PERF_SEED_COUNT` env vars are not treated as evidence of a working "heavy" profile.
- **`test-summary` (added in v3, per Owner round-2 Finding 1):** remove `performance-budget`/`performance-heavy` from its `needs:` list (currently `[accessibility-tests, performance-budget, performance-heavy, lighthouse-ci, e2e-tests]`) and remove the two corresponding "Performance Budget Tests"/"Performance Heavy Tests" sections from its generated summary body. This is retirement housekeeping, not scope creep: `test-summary` is a real reporting consumer of the two retired jobs (a dangling `needs:` reference to a deleted job is a hard workflow-validation failure, not a soft no-op), but it is not a reason to preserve the jobs — v2 incorrectly stated no consumer existed at all; the correction is to update the one real consumer in the same change, not to keep the phantom jobs alive for its sake. No other job in the workflow is touched.
- If genuine budget/heavy performance tiers are wanted later, that is a separately scoped future work item requiring real threshold/load definitions and its own evidence — not an extension of this gap.
- **Option A is withdrawn as the recommendation** (repointing the two phantom jobs at `--group performance` relocates the falsehood rather than removing it — the prior description of this as "honest relabeling" was incorrect, since no label was actually changed). **Option B** (backfill real budget/heavy tests) remains a legitimate future enhancement but is out of scope here — defining real performance thresholds/load profiles is a product/ops decision, not a CI-truthfulness fix, and Owner's direction explicitly excludes inventing tests under GAP-041. Full comparison table (correctness/fail-closed/maintainability/regression/scope/reversibility) is in the engineering spec §3.
- **Implementation-mechanism note:** `performance-tests` invokes tests via `php artisan test`, not `vendor/bin/phpunit` directly; whether that wrapper passes `--fail-on-empty-test-suite` through unmodified has not been verified at Gate 2. The contract is behavioral (zero intended tests selected must cause non-zero failure), not a fixed CLI spelling — the future implementation plan must verify the wrapper's behavior first and may substitute an equivalent direct PHPUnit invocation if needed. Deferred to implementation, not a Gate-2 decision point.

## Gate 3 acceptance contract (revised in v3 — retirement proof expanded to cover `test-summary`)

Under the recommended Option D, **Model B** applies (see engineering spec §5 for both models in full): because `performance-budget`/`performance-heavy` no longer exist as jobs, they carry no PHPUnit live-execution requirement. Instead Gate 3 must prove, on the exact implementation tree:

**(A) Phantom-tier retirement proof (static):** no `performance-budget` or `performance-heavy` job definition remains; `test-summary.needs` no longer references either; the generated Test Summary no longer reports either; no dangling `${{ needs.performance-budget.* }}`/`${{ needs.performance-heavy.* }}` expression remains anywhere; no stale tier-specific artifact reference remains; the resulting workflow is syntactically/structurally valid (basic YAML-parse check as this repo's available baseline — no dedicated `actionlint`/`yamllint` is currently wired in — plus, as the strongest evidence, a real GitHub Actions dispatch that reaches and completes `test-summary` without a `needs:`-resolution error).

**(B) Surviving `performance-tests` LIVE proof, mandatory and non-waivable:** job name/description matches what it tests; the intended population is real and traceable to the actual matrix-selected files, on each matrix leg; **≥1 test from that population executes in a LIVE GitHub Actions run** on the exact implementation tree, per leg (JUnit/log artifact, not local reproduction); the real-MySQL fail-closed preflight succeeds live before the tests; zero-selection causes non-zero failure via the chosen fail-closed mechanism, proven **live** using a disposable proof branch/commit that reintroduces a zero-selection condition, shown to fail the job on a real run, then cleanly reverted; evidence classification (LIVE/STATIC/LOCAL/HISTORICAL) used honestly throughout.

No Gate-3-complete state is permitted while any LIVE-classified criterion is still pending. (If Option A or B were chosen instead, Model A would apply and Gate 3 for `performance-budget`/`performance-heavy` would be BLOCKED until the missing-script defect is independently resolved — not applicable under the recommended Option D, stated here only for completeness.)

## The masked-job problem — disposition under Option D (unchanged from v2)

Retiring `performance-budget`/`performance-heavy` removes their dependency on the missing `.github/scripts/ci_prepare_testing_env.sh` from **GAP-041's own Gate-3 path** — there is no longer a PHPUnit step under those job names whose live reachability this gap needs to prove.

## Missing-script defect — disposition (corrected in v3, per Owner round-2 Finding 2)

**v2's disposition was wrong** and is reversed here. An exhaustive repo-wide search (engineering spec §6) found exactly two references to `.github/scripts/ci_prepare_testing_env.sh` anywhere in the repository — both inside `performance-budget`/`performance-heavy`'s own steps. `accessibility-tests`, `e2e-tests`, and `lighthouse-ci` each have their own independent environment setup and do not call this script at all; v2's claim that the missing script also blocks them (carried forward from Gate 1's audit §1.4) does not match exact current code, and Gate 1's original framing conflated "failed together in one historical CI run" with "failed for the same code-level reason" — a narrow epistemic correction to that effect is recorded in the engineering spec §9, without reopening Gate 1's approved core finding.

Since retiring `performance-budget`/`performance-heavy` (Option D) removes the script's only two callers, **a separate Operational Gap Register entry for this missing script is not warranted based on current evidence** — an unreferenced, nonexistent file with zero remaining callers is not itself an actionable runtime defect. This reverses the v1/v2 recommendation.

## Scope boundaries (revised in v3 — `test-summary` cleanup added)

**In scope for implementation (Gate 3, not authorized yet):** `performance-tests`' CLI invocation (`--group performance` + a fail-closed mechanism per the implementation-mechanism note above); deletion of the `performance-budget`/`performance-heavy` job definitions; the consequential `test-summary` cleanup (both `needs:` entries and both summary sections); a live-dispatch verification proof per the acceptance contract above. **Out of scope:** performance thresholds/budgets, new/backfilled budget/heavy tests (Option B), any reorganization of `accessibility-tests`/`lighthouse-ci`/`e2e-tests` beyond what's strictly dangling after retirement, application/business/RBAC/tenant/production-schema code, GAP-040, GAP-042, `.github/scripts/ci_prepare_testing_env.sh` (moot after Option D, not separately registered or fixed), and any branch-protection policy change. No proposed change touches production/business/tenant/RBAC semantics, so no Design Dependency Preflight is triggered.

## What the owner is NOT being asked to decide

Owner is not being asked to approve any code/workflow change at this step — only whether Option D, with its v3-expanded implementation surface (repair `performance-tests`; retire the two phantom tier jobs; clean up `test-summary`'s consequential references), is the right target; whether the revised Gate 3 acceptance contract (Model B, retirement proof + full LIVE contract, no LIVE/STATIC-pending exception) is correct; whether the reversed missing-script disposition (no separate gap warranted) is acceptable; and whether the narrow Gate-1 epistemic correction (engineering spec §9) is acceptable as recorded. No implementation plan, no code, and no Gate 3 are authorized by this document.
