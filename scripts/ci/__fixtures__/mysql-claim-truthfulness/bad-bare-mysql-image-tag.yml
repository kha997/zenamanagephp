name: Fixture — bad, bare mysql image (no tag)
on: push
jobs:
  bad-bare-tag-job:
    runs-on: ubuntu-latest
    env:
      DB_CONNECTION: mysql
    services:
      mysql:
        image: mysql
    steps:
      - run: php artisan test
